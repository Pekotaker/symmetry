h1 = t('mailer.confirmation.subject')

p = t('mailer.greeting', name: @resource.first_name.presence || @resource.email)

p = t('mailer.confirmation.body')

p style="text-align: center; margin: 24px 0;"
  = link_to t('mailer.confirmation.action'), confirmation_url(@resource, confirmation_token: @token), class: 'button'

p style="font-size: 14px; color: #71717a;"
  = t('mailer.confirmation.ignore')
