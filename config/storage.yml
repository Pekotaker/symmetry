test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Cloudinary for production image storage
cloudinary:
  service: Cloudinary
  folder: <%= Rails.env %>
